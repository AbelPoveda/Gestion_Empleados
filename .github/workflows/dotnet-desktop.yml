name: .NET CI

on:
  push:
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET 10
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore "GestionDeEmpleados.slnx"

    - name: Build
      run: dotnet build "GestionDeEmpleados.slnx" --no-restore --configuration Release

    - name: Test
      run: dotnet test "Gestion_de_Empleados.Tests/Gestion_de_Empleados.Tests.csproj" --configuration Release --verbosity normal
